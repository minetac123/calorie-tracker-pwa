node_modules/

# Generated by `npm run build` — staging dir for the Capacitor iOS bundle.
# The iOS project itself (ios/) IS committed: the macOS CI runner builds from
# it and there is no Mac here to regenerate it.
www/

# Secrets — never commit these. Copy .env.example to .env.local and fill it in.
.env
.env.*
!.env.example

# Vercel
.vercel/

# Interní pracovní soubory agentů a lokální poznámky
.agents/
scratchpad/
ORIGINAL_REQUEST.md

# OS / editor
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/
